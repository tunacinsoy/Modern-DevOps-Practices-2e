# venv was created to run the python app locally
ch3/docker-compose/venv/*
ch7/knative/venv/*

# Credentials for the service principal resource
ch8-terraform/az_sp_terraform.json

# Terraform variable values and provider binary downloaded after 'terraform init'
*.tfvars
.terraform*

# Terraform detailed execution plan binary after running 'terraform plan'
*.tfplan

# Terraform tfstate files
*.tfstate
*.tfstate.backup
